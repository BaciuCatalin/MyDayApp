export class Ingredient{
     // ingredientID: number;
    // recipeId: number;
    public name: string;
    public amount: number;

    constructor(name: string, amount: number){
        this.name = name;
        this.amount = amount;
    }
}